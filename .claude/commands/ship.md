Stage all changes and push to the remote repository immediately — no diff or status checks.

Steps:
1. Run `git add -A` to stage all changes (skip dist/, node_modules/).
2. Write a concise commit message in Korean using conventional commit style (feat/fix/chore/etc) based on the filenames being staged.
3. Commit and run `git push`.
4. Report the commit hash to the user.
